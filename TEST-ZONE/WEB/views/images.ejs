<!DOCTYPE html>

<html>
    <body>
        <section>
            <article class="center-block">  
				<h1>Images on private repository </h1>
				<% if(images.code) { %>
				<p>We are currently experiencing some problems with the service.</p>
				<p>cause : <%= images.code %></p>

				<% } else { %>
				<form class="has-feedback" style="width:50%;" name="input" role="search">
					<input type="search" class="form-control" name="search" placeholder="Search..." autofocus>
					<span class="glyphicon form-control-feedback glyphicon-search"></span>
					<label class="control-label"><%= text %></label>
				</form>
				
				<table class="table table-striped table-condensed">
					<thead>
						<tr>
							<th>Name</th>
							<th>Description</th>
						</tr>
					</thead>
					<tbody>
						<% for(var i=0; i<images.num_results; i++) {%>
							<tr>
								<td><%= images.results[i].name.replace('library/', server_addr+":5000/") %></td>
								<td><%= images.results[i].description %></td>
							</tr>
						<% } %>
					</tbody>
				</table>
				<% } %>
			</article>
		<section>
     </body>
</html>

