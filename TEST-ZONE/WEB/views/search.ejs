<!DOCTYPE html>
<html>	                	 
    <body>
        <section>
            <article>  
                          
			<h1><%= title %> page </h1>
			
			<body>
				<div class="table-responsive">
					<table id="searchtable" class="table">
					
					<% var serverList = []; %>
					<% for (var server in servers) { %>
					<% serverList.push(servers[server]); %>
					<% }; %>
					<script>
						
						UpdateTable('[<%= serverList %>]', 'searchtable');
					</script>
					</table>
					
					<button class="btn btn-default" onclick="PerformSearch()">Search</button>
				</div>
			</body>               

            </article>
        </section>
    </body>
</html>






